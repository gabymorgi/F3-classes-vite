crea un nuevo proyecto con vite
https://vitejs.dev/guide/

crea un nuevo repositorio en github
asegurate de que el gitingore contenga node_modules

sube el proyecto a github

realiza cambios en algun archivo y fijate como se visualiza
en el source control de vscode
agrega un nuevo archivo .env en el src y vuelve a observar los cambios
ahora agrega .env al gitingore y vuelve a observar los cambios

instala la siguiente dependencia
https://www.npmjs.com/package/is-odd
npm i is-odd
utilizala en el proyecto
import isOdd from 'is-odd';
console.log(isOdd(1)); //=> true

observa nuevamente los cambios en el source control de vscode

sube los cambios a github